name: fr_mvvm_locale
description: "Locale switching helpers for FlowR MVVM Flutter apps, including a locale view model, formatter extensions, and menu UI widgets."
version: 2.0.1
repository: "https://github.com/Hu-Wentao/flowr"
topics:
  - flowr
  - localization

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flowr: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
