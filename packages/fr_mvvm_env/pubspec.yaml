name: fr_mvvm_env
description: "Environment switching helpers for FlowR MVVM Flutter apps, including a view model contract and dropdown UI for dev/staging/prod state."
version: 2.0.2
repository: "https://github.com/Hu-Wentao/flowr"
topics:
  - flowr
  - environment

environment:
  sdk: ^3.7.0
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flowr: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
