name: flowr_dart
description: "Pure Dart reactive state management primitives for FlowR, built on bloc with update, error handling, logging, and stream helpers."
version: 4.0.2
repository: "https://github.com/Hu-Wentao/flowr"
topics:
  - flowr
  - state-management
  - reactive
  - bloc
platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows:

environment:
  sdk: ^3.7.0

resolution: workspace

dependencies:
  async: ^2.11.0
  bloc: ^9.0.0
  stack_trace: ^1.11.1
  meta: ^1.16.0
  slowly: ^0.4.3
  logging: ^1.3.0

dev_dependencies:
  lints: ^5.1.0
  test: ^1.25.0
